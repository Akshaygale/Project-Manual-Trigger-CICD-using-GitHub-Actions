name: Manual CI/CD Deployment

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Login DockerHub
      run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin

    - name: Read version
      id: version
      run: echo "VERSION=$(cat VERSION)" >> $GITHUB_ENV

    - name: Build & Push
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/demoapp:latest .
        docker push ${{ secrets.DOCKER_USERNAME }}/demoapp:latest

    - name: Deploy to EC2
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_KEY }}
        script: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/demoapp:latest
          docker stop demo || true
          docker rm demo || true
          docker run -d -p 5000:5000 --name demo ${{ secrets.DOCKER_USERNAME }}/demoapp:${{ env.VERSION }}
